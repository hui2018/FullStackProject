In Main.html it will be a front-end navigation bar where user can click on the bar and it will go to
a different tab that contains different information. The navigation will be implemented using bootstrap.

To display the new tab, the user will have to enter the "admin" into the user and password field under the login tab.

Each day there will be an information of the location and the map. Each information and map will have an link for
more information.

Budget calculator will be available when the user login with admin. The calculation is a simple estimate of how much it would
cost to travel with some basic information